spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_sboot
    username: aegerm
    password: sysdb
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
    generate-ddl: true